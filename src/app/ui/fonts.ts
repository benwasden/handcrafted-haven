import { Funnel_Display, Montserrat } from 'next/font/google';

export const funnel = Funnel_Display({ subsets: ['latin'] });
export const montserrat = Montserrat({ subsets: ['latin'], weight: ['400', '700'] }); 